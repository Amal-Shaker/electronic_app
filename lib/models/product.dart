class Product {
  int id, price;
  String title, subTitle, description, image;

  Product(
      {this.id,
      this.description,
      this.image,
      this.price,
      this.subTitle,
      this.title});
}
